// BANNER CONFIGURATION
// Set ENABLED to false to hide the banner completely
// Customize the text and background color below

const BANNER_CONFIG = {
  // Set to true to show banner, false to hide it
  ENABLED: true,
  
  // Banner text content - HTML is allowed (e.g., "<strong>ANNOUNCEMENT</strong>: New games!")
  TEXT: "Big updates are coming.",
  
  // Background color - any valid CSS color (hex, rgb, hsl, color name)
  BACKGROUND_COLOR: "#ffd966",
  
  // Text color
  TEXT_COLOR: "#1e3a5f",
  
  // Optional: Add a link (set to empty string "" for no link)
  LINK_URL: "",
  
  // Optional: Link text (only used if LINK_URL is set)
  LINK_TEXT: "Check them out →"
};